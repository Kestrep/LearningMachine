{{ form_start(form) }}
    {{ form_widget(form) }}
        <div class="buttons d-cc">
            <div class="btn-darkgrey">Revenir</div>
            <button class="btn-orange">{{ button_label|default('Save') }}</button>
        </div>
{{ form_end(form) }}