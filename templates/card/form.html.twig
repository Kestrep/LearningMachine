{% extends 'base.html.twig' %}

{% block title %}
    {% if edit %}
        Edit Card
    {% elseif not edit %}
        New Card
    {% endif %}
{% endblock %}

{% block body %}
<main>
    <h1>
    {% if edit %}
        Modifier une carte
    {% elseif not edit %}
        Créer une carte
    {% endif %}
    </h1>

    {{ include('card/_form.html.twig', {'edit': edit}) }}

    {# <div class="debug">
        <input type="text" name="" id="loremCopy-WordsCount" value="52">
        <div class="btn-orange loremCopy-Button" data-count="loremCopy-WordsCount">Co*pie Lorem</div>
        <div class="btn-orange request-new-button">New Card</div>
    </div> #}

    {#
        <form method="post" action="{{ path('card_delete', {'id': card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card.id) }}">
            <button class="btn">Delete</button>
        </form>
    #}
</main>

{% endblock %}