{% extends 'base.html.twig' %}

{% if edit %}
    {% set title = "Modifier la catégorie" %}
{% else %}
    {% set title = "Créer une catégorie" %}
{% endif %}


{% block title %}{{ title }}{% endblock %}

{% block body %}
<main>
    <h1>{{ title }}</h1>

    {{ include('category/_form.html.twig', {'edit': edit}) }}
    {#
        <form method="post" action="{{ path('category_delete', {'id': category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
            <button class="btn">Delete</button>
        </form>
    #}
</main>
{% endblock %}
