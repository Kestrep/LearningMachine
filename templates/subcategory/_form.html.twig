{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn-orange submit-button" data-url="{{ path('subcategory_new') }}">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
